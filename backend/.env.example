APP_PORT=4000
APP_HOST=0.0.0.0
APP_URL=http://localhost:4000
JWT_SECRET=change-me
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=venicars
POSTGRES_PASSWORD=venicars
POSTGRES_DB=venicars
DB_SYNCHRONIZE=true
REDIS_URL=redis://localhost:6379

# OSRM Map Matching Service (optional)
# 启用后，轨迹查询会自动将 GPS 点贴合到道路网络
# 如果不配置，系统会优雅降级，返回原始 GPS 轨迹
MAP_MATCHER_URL=http://localhost:5000
